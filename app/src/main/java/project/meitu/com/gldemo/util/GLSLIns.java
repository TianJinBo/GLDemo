package project.meitu.com.gldemo.util;

/**
 * GLSL是openGL的着色语言，以下为一些语法说明
 */
public class GLSLIns {
    //attribute  属性
    //uniform ？？
    //varying ？？
    //vec4    包含4各个向量的分量  位置：(x,y,z,w) 默认值(0,0,0,1)       颜色：(红，绿，蓝，透明度)

    // 精度限定符 lowp mediump highp
}
